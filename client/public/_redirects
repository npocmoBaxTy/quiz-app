# SPA-fallback для статического хостинга (Render, Netlify).
# Vite копирует public/ в dist/ как есть, поэтому файл попадает в публикуемый
# каталог и правило едет вместе с кодом, а не живёт в настройках сервиса.
#
# 200, а не 301: адрес в строке браузера должен остаться прежним, иначе
# react-router потеряет маршрут. Хэшированные файлы из /assets отдаются
# раньше этого правила — оно срабатывает только когда файла нет.
/*    /index.html   200
